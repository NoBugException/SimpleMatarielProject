<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matariel.simplematarielproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".swipedismissrecyclerview.SwipeDismissRecyclerActivity" />
        <activity android:name=".flabbyview.FlabbyActivity" />
        <activity android:name=".stickyheader.StickyHeaderActivity" />
        <activity android:name=".nice_spinner.NiceSpinnerActivity" />
        <activity android:name=".parallax.ParallaxActivity" />
        <activity android:name=".pulltozoom.PullToZoomScrollActivity" />
        <activity android:name=".pulltozoom.PullToZoomListActivity" />
        <activity android:name=".pulltozoom.recyclerview.PullToZoomRecyclerActivity" />
        <activity android:name=".pulltozoom.PullToZoomActivity" />
    </application>

</manifest>